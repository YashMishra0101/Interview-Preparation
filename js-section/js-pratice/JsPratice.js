let isEvenCheck = (value) => {
  let check = value % 2 === 0;
  console.log(check);
  check ? console.log("Even Number") : console.log("Not a Even Number");
};

isEvenCheck(10);

let printEven=(number)=>{
    let check=0;
    if(check<=number){
        for(let value=0; value<=number; value++ ){
            
        }
    }
}